
services:

  postgre:
    container_name: postgre
    build:
      context: ./postgre
      dockerfile: Dockerfile
      args:
        PG_MAJOR: ${PG_MAJOR}
    env_file:
      - .env
    environment:
      - POSTGRES_USER=${POSTGRE_USER}
      - POSTGRES_PASSWORD=${POSTGRE_PASSWORD}
      - POSTGRES_DB=${POSTGRE_DB}
    ports:
      - "${POSTGRE_PORT}:${POSTGRE_PORT}"
    volumes:
      - postgre-data:/var/lib/postgresql/data
    networks:
      - shared-network
    restart: always

  minio:
    container_name: minio
    image: minio/minio:latest
    env_file:
      - .env
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
      - MINIO_PORT=${MINIO_PORT}
      - MINIO_CONSOLE_PORT=${MINIO_CONSOLE_PORT}
      - MINIO_SERVER_URL=http://minio:${MINIO_PORT}
    command: server --address :${MINIO_PORT} --console-address :${MINIO_CONSOLE_PORT} /data
    ports:
      - "${MINIO_PORT}:${MINIO_PORT}"
      - "${MINIO_CONSOLE_PORT}:${MINIO_CONSOLE_PORT}"
    volumes:
      - minio-data:/data
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.minio.rule=Host(`minio.mlops.bzh`)"
      - "traefik.http.routers.minio.entrypoints=websecure"
      - "traefik.http.routers.minio.tls.certresolver=cloudflare"
      - "traefik.http.routers.minio.middlewares=authelia@docker,secureHeaders@docker"
    networks:
      - shared-network
    restart: always

  minio-client:
    container_name: minio-client
    build:
      context: ./minio-client
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      - MINIO_ENDPOINT=${MINIO_API_URL}
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
    ports:
      - "${MINIO_CLIENT_PORT}:${MINIO_CLIENT_PORT}"
    volumes:
      - ./label-studio/cors.json:/label-studio/cors.json
    networks:
      - shared-network
    depends_on:
      - minio
    restart: always

  neo4j:
    image: neo4j:latest
    container_name: neo4j
    ports:
      - "7474:7474"  # Port for Neo4j web interface
      - "7687:7687"  # Port for Neo4j Bolt protocol
    environment:
      - NEO4J_AUTH=${NEO4J_AUTH}

      - NEO4J_PLUGINS=["apoc"]
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.neo4j.rule=Host(`neo4j.mlops.bzh`)"
      - "traefik.http.routers.neo4j.entrypoints=websecure"
      - "traefik.http.routers.neo4j.tls.certresolver=cloudflare"
      - "traefik.http.routers.neo4j.middlewares=authelia@docker,secureHeaders@docker"
      - "traefik.http.services.minio.loadbalancer.server.port=${MINIO_CONSOLE_PORT}"

    networks:
      - shared-network
    restart: unless-stopped 

  mlflow:
    container_name: mlflow
    build:
      context: ./mlflow
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      - MLFLOW_BACKEND_STORE_URI=${MLFLOW_BACKEND_STORE_URI}
      - MLFLOW_ARTIFACT_ROOT=${MLFLOW_ARTIFACT_ROOT}
      - MLFLOW_S3_ENDPOINT_URL=${MLFLOW_S3_ENDPOINT_URL}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
    ports:
      - "${MLFLOW_PORT}:${MLFLOW_PORT}"
    volumes:
      - ./mlflow:/mlflow
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.mlflow.rule=Host(`mlflow.mlops.bzh`)"
      - "traefik.http.routers.mlflow.entrypoints=websecure"
      - "traefik.http.routers.mlflow.tls.certresolver=cloudflare"
      - "traefik.http.routers.mlflow.middlewares=authelia@docker,secureHeaders@docker"
    networks:
      - shared-network
    restart: always

  label-studio:
    container_name: label-studio
    build:
      context: ./label-studio
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      - LABEL_STUDIO_PORT=${LABEL_STUDIO_PORT}
      - LABEL_STUDIO_BUCKET_ENDPOINT_URL=${LABEL_STUDIO_BUCKET_ENDPOINT_URL}
      - LABEL_STUDIO_BUCKET_ACCESS_KEY=${LABEL_STUDIO_BUCKET_ACCESS_KEY}
      - LABEL_STUDIO_BUCKET_SECRET_KEY=${LABEL_STUDIO_BUCKET_SECRET_KEY}
      - LABEL_STUDIO_ROOT_URL=/labelstudio/
      - DJANGO_CSRF_TRUSTED_ORIGINS=https://labelstudio.mlops.bzh
      - CSRF_COOKIE_SECURE=1
      - POSTGRE_HOST=${POSTGRE_HOST}
      - POSTGRE_PORT=${POSTGRE_PORT}
      - POSTGRE_USER=${POSTGRE_USER}
      - POSTGRE_PASSWORD=${POSTGRE_PASSWORD}
      - POSTGRE_DB=${POSTGRE_DB}
    ports:
      - "${LABEL_STUDIO_PORT}:${LABEL_STUDIO_PORT}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - label-studio-data:/label-studio/data
      - ./label-studio/create_bucket.py:/label-studio/create_bucket.py
      - ./label-studio/cors.json:/label-studio/cors.json
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.labelstudio.rule=Host(`labelstudio.mlops.bzh`)"
      - "traefik.http.routers.labelstudio.entrypoints=websecure"
      - "traefik.http.routers.labelstudio.tls.certresolver=cloudflare"
      - "traefik.http.routers.labelstudio.middlewares=authelia@docker,secureHeaders@docker"
    networks:
      - shared-network
    restart: always


  # gliner:
  #   container_name: gliner
  #   image: humansignal/ml-backend:v0
  #   build:
  #     context: ./label-studio-ml-backend/gliner
  #     args:
  #       TEST_ENV: ${TEST_ENV}
  #   env_file:
  #     - .env
  #   environment:
  #     - BASIC_AUTH_USER=${GLINER_BASIC_AUTH_USER}
  #     - BASIC_AUTH_PASS=${GLINER_BASIC_AUTH_PASS}
  #     - LABEL_STUDIO_ML_BACKENDS=${LABEL_STUDIO_ML_BACKENDS}
  #     - LOG_LEVEL=DEBUG
  #     - ANY=PARAMETER
  #     - WORKERS=1
  #     - THREADS=8
  #     - MODEL_DIR=/mlflow/artifacts/models
  #     - LABEL_STUDIO_URL=http://host.docker.internal:${LABEL_STUDIO_PORT}
  #     - LABEL_STUDIO_API_KEY=${LABEL_STUDIO_API_KEY}
  #     - HF_HOME=/app/huggingface_cache
  #   ports:
  #     - "${MLBACKEND_PORT}:${MLBACKEND_PORT}"
  #   depends_on:
  #     - label-studio
  #   volumes:
  #     - "./data/server:/data"
  #     - ~/.cache/huggingface:/app/huggingface_cache

  #   networks:
  #     - shared-network

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    volumes:
      - ./metrics/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - shared-network
    restart: always
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.prometheus.rule=Host(`prometheus.mlops.bzh`)"
      - "traefik.http.routers.prometheus.entrypoints=websecure"
      - "traefik.http.routers.prometheus.tls.certresolver=cloudflare"
      - "traefik.http.routers.prometheus.middlewares=authelia@docker,secureHeaders@docker"

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin_password  # Changez ceci pour des raisons de sécurité
    ports:
      - "3001:3001"
    volumes:
      - grafana-data:/var/lib/grafana
    networks:
      - shared-network
    restart: always
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.grafana.rule=Host(`grafana.mlops.bzh`)"
      - "traefik.http.routers.grafana.entrypoints=websecure"
      - "traefik.http.routers.grafana.tls.certresolver=cloudflare"
      - "traefik.http.routers.grafana.middlewares=authelia@docker,secureHeaders@docker"

volumes:
  postgre-data:
  minio-data:
  label-studio-data:
  gliner-data:
  mlflow-data:
  neo4j_data:
  neo4j_logs:
  grafana-data:

networks:
  shared-network:
    external: true
